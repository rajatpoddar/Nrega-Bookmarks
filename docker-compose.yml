version: '3.8'

services:
  nrega-bookmarks:
    build: .
    container_name: nrega_bookmarks_app
    restart: unless-stopped
    ports:
      - "6006:5000"  # Yahan 6000 ko 6001 kar diya hai
    volumes:
      - ./instance:/app/instance